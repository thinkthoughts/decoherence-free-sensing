[project]
name = "decoherence-free-sensing"
version = "0.1.0"
description = "Computational specifications for decoherence-free differential quantum sensing."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
